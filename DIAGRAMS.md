# 📊 DIAGRAM & VISUALISASI
## Sistem Pemesanan Hotel - Arsitektur OOP

---

## 🏗️ CLASS DIAGRAM

```
┌─────────────────────────────────────────────────────────┐
│                    Room (Abstract)                      │
│─────────────────────────────────────────────────────────│
│ - _room_id: str                                         │
│ - _room_number: str                                     │
│ - _capacity: int                                        │
│ - _base_price: float                                    │
│ - _is_available: bool                                   │
│─────────────────────────────────────────────────────────│
│ + @property room_id(): str                              │
│ + @property room_number(): str                          │
│ + @property capacity(): int                             │
│ + @property base_price(): float                         │
│ + @property is_available(): bool                        │
│ + @is_available.setter(value: bool)                     │
│ + @abstractmethod get_room_type(): str                  │
│ + @abstractmethod calculate_price(nights: int): float   │
│ + @abstractmethod get_amenities(): List[str]            │
│ + to_dict(): Dict                                       │
└──────────────────────┬──────────────────────────────────┘
                       │
                       │ Inheritance
        ┌──────────────┼──────────────┐
        │              │              │
┌───────▼───────┐ ┌────▼─────┐ ┌─────▼──────┐
│ StandardRoom  │ │DeluxeRoom│ │ SuiteRoom  │
├───────────────┤ ├──────────┤ ├────────────┤
│ capacity: 2   │ │capacity:3│ │capacity: 4 │
│ price:500K    │ │price:800K│ │price: 1.5M │
├───────────────┤ ├──────────┤ ├────────────┤
│+ get_room_type│ │+ get_room│ │+ get_room  │
│  → "Standard" │ │  → "Delux│ │  → "Suite" │
│               │ │     e"   │ │            │
│+ calculate_   │ │+ calc... │ │+ calc...   │
│  price()      │ │  (10%    │ │  (15%      │
│  → base*nights│ │  discount│ │  discount) │
│               │ │  >3 night│ │            │
│+ get_ameniti│ │+ get_amen│ │+ get_amen  │
│  es()         │ │  ities() │ │  ities()   │
│  → 5 items    │ │  → 7 item│ │  → 9 items │
└───────────────┘ └──────────┘ └────────────┘


┌─────────────────────┐        ┌──────────────────────┐
│      User           │        │      Booking         │
├─────────────────────┤        ├──────────────────────┤
│ - _user_id: str     │        │ - _booking_id: str   │
│ - _username: str    │        │ - _user_id: str      │
│ - _password: str    │        │ - _room_id: str      │
│ - _role: str        │        │ - _check_in: str     │
│ - _full_name: str   │        │ - _check_out: str    │
├─────────────────────┤        │ - _nights: int       │
│ + check_password()  │        │ - _total_price: float│
│ + is_admin(): bool  │        │ - _guest_name: str   │
│ + to_dict(): Dict   │        │ - _guest_phone: str  │
└─────────────────────┘        │ - _status: str       │
                               ├──────────────────────┤
                               │ + to_dict(): Dict    │
                               └──────────────────────┘
```

---

## 🔄 POLYMORPHISM VISUALIZATION

### Perhitungan Harga (calculate_price method)

```
Booking 2 Malam:
┌────────────┬──────────────┬─────────┬──────────────┐
│ Room Type  │ Base Price   │ Nights  │ Total        │
├────────────┼──────────────┼─────────┼──────────────┤
│ Standard   │ Rp 500,000   │   2     │ Rp 1,000,000 │
│ Deluxe     │ Rp 800,000   │   2     │ Rp 1,600,000 │
│ Suite      │ Rp 1,500,000 │   2     │ Rp 3,000,000 │
└────────────┴──────────────┴─────────┴──────────────┘

Booking 5 Malam (Ada Diskon!):
┌────────────┬──────────────┬─────────┬──────────────┬────────────┐
│ Room Type  │ Base Price   │ Nights  │ Before Disc  │ Final      │
├────────────┼──────────────┼─────────┼──────────────┼────────────┤
│ Standard   │ Rp 500,000   │   5     │ Rp 2,500,000 │ 2,500,000  │
│            │              │         │              │ (no disc)  │
├────────────┼──────────────┼─────────┼──────────────┼────────────┤
│ Deluxe     │ Rp 800,000   │   5     │ Rp 4,000,000 │ 3,600,000  │
│            │              │         │              │ (10% off)  │
├────────────┼──────────────┼─────────┼──────────────┼────────────┤
│ Suite      │ Rp 1,500,000 │   5     │ Rp 7,500,000 │ 6,375,000  │
│            │              │         │              │ (15% off)  │
└────────────┴──────────────┴─────────┴──────────────┴────────────┘
```

**Implementasi:**
```python
# StandardRoom - No discount
def calculate_price(self, nights: int) -> float:
    return self._base_price * nights

# DeluxeRoom - 10% discount for >3 nights
def calculate_price(self, nights: int) -> float:
    total = self._base_price * nights
    if nights > 3:
        total *= 0.9  # 10% discount
    return total

# SuiteRoom - 15% discount for >3 nights
def calculate_price(self, nights: int) -> float:
    total = self._base_price * nights
    if nights > 3:
        total *= 0.85  # 15% discount
    return total
```

---

## 🔐 ENCAPSULATION VISUALIZATION

```
┌─────────────────────────────────────────────────┐
│              Room Object                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  🔒 Private Attributes (Internal State)         │
│  ┌───────────────────────────────────────────┐ │
│  │ _room_id = "R001"                         │ │
│  │ _room_number = "101"                      │ │
│  │ _capacity = 2                             │ │
│  │ _base_price = 500000                      │ │
│  │ _is_available = True                      │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  📖 Public Interface (Controlled Access)        │
│  ┌───────────────────────────────────────────┐ │
│  │ @property                                 │ │
│  │ def room_id(self):                        │ │
│  │     return self._room_id  ← Read Only     │ │
│  │                                           │ │
│  │ @property                                 │ │
│  │ def is_available(self):                   │ │
│  │     return self._is_available  ← Getter   │ │
│  │                                           │ │
│  │ @is_available.setter                      │ │
│  │ def is_available(self, value):            │ │
│  │     self._is_available = value  ← Setter  │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
└─────────────────────────────────────────────────┘

Usage:
  ❌ room._room_id = "R999"  # Don't access directly!
  ✅ print(room.room_id)      # Use getter
  ✅ room.is_available = False  # Use setter
```

---

## 🌊 DATA FLOW DIAGRAM

### User Login Flow
```
┌──────┐    POST      ┌──────────┐   authenticate   ┌───────┐
│Login │  username    │  Flask   │───────────────→  │utils  │
│ Page │  password    │  Route   │                  │.py    │
└──┬───┘  ─────────→  └────┬─────┘                  └───┬───┘
   │                       │                            │
   │                       │                            ▼
   │                       │                     ┌──────────────┐
   │                       │                     │ load_users() │
   │                       │                     │ from JSON    │
   │                       │                     └──────┬───────┘
   │                       │                            │
   │                       │      User object           │
   │                       │  ◄─────────────────────────┘
   │                       │
   │                       ▼
   │                  ┌─────────────┐
   │                  │ Set Session │
   │                  │ - user_id   │
   │                  │ - username  │
   │                  │ - role      │
   │                  └──────┬──────┘
   │                         │
   │   Redirect              │
   │  ◄──────────────────────┘
   │
   ▼
┌──────────┐
│Dashboard │
└──────────┘
```

### Booking Creation Flow
```
┌──────────┐  Submit    ┌──────────┐  create_booking  ┌───────┐
│ Booking  │  Form      │  Flask   │─────────────────→│utils  │
│ Form     │  ────────→ │  Route   │                  │.py    │
└──────────┘            └──────────┘                  └───┬───┘
                                                          │
                        ┌─────────────────────────────────┘
                        │
                        ▼
                 ┌──────────────┐
                 │ get_room_by_id│
                 │ (polymorphism)│
                 └──────┬────────┘
                        │
                        ▼
                 ┌─────────────────┐
                 │ room.calculate_ │
                 │ price(nights)   │  ← Polymorphic method
                 └──────┬──────────┘
                        │
                        ▼
                 ┌─────────────────┐
                 │ Create Booking  │
                 │ object          │
                 └──────┬──────────┘
                        │
                        ▼
                 ┌─────────────────┐
                 │ save_bookings() │
                 │ to JSON         │
                 └──────┬──────────┘
                        │
                        ▼
                 ┌─────────────────┐
                 │ Update room     │
                 │ availability    │
                 └──────┬──────────┘
                        │
                        ▼
                 ┌─────────────────┐
                 │ log_activity()  │
                 │ to app.log      │
                 └─────────────────┘
```

---

## 🗂️ FILE STRUCTURE & RELATIONSHIPS

```
app.py (Main Application)
  │
  ├──> models.py (OOP Classes)
  │     │
  │     ├──> Room (Abstract)
  │     │     ├──> StandardRoom
  │     │     ├──> DeluxeRoom
  │     │     └──> SuiteRoom
  │     │
  │     ├──> User
  │     └──> Booking
  │
  ├──> utils.py (Data Operations)
  │     │
  │     ├──> User Management
  │     │     ├─ load_users()
  │     │     ├─ save_users()
  │     │     ├─ authenticate_user()
  │     │     └─ get_user_by_id()
  │     │
  │     ├──> Room Management (CRUD)
  │     │     ├─ load_rooms()
  │     │     ├─ save_rooms()
  │     │     ├─ create_room()
  │     │     ├─ get_room_by_id()
  │     │     ├─ update_room_availability()
  │     │     └─ delete_room()
  │     │
  │     ├──> Booking Management (CRUD)
  │     │     ├─ load_bookings()
  │     │     ├─ save_bookings()
  │     │     ├─ create_booking()
  │     │     ├─ get_booking_by_id()
  │     │     ├─ update_booking_status()
  │     │     └─ delete_booking()
  │     │
  │     └──> Logging
  │           └─ log_activity()
  │
  └──> templates/ (HTML Templates)
        │
        ├──> base.html (Master Template)
        │
        ├──> Authentication
        │     └─ login.html
        │
        ├──> Dashboard
        │     └─ dashboard.html
        │
        ├──> Room Management
        │     ├─ rooms.html
        │     ├─ add_room.html
        │     └─ edit_room.html
        │
        ├──> Booking Management
        │     ├─ bookings.html
        │     ├─ add_booking.html
        │     ├─ edit_booking.html
        │     └─ booking_detail.html
        │
        └──> Admin
              └─ logs.html

data/ (JSON Storage)
  ├──> users.json
  ├──> rooms.json
  └──> bookings.json

app.log (Audit Trail)
```

---

## 🎭 ROLE-BASED ACCESS MATRIX

```
┌──────────────────────┬──────────┬──────────┐
│      Feature         │  Admin   │   Tamu   │
├──────────────────────┼──────────┼──────────┤
│ Dashboard            │    ✅    │    ✅    │
├──────────────────────┼──────────┼──────────┤
│ View Rooms           │    ✅    │    ✅    │
│ Add Room             │    ✅    │    ❌    │
│ Edit Room            │    ✅    │    ❌    │
│ Delete Room          │    ✅    │    ❌    │
├──────────────────────┼──────────┼──────────┤
│ View All Bookings    │    ✅    │    ❌    │
│ View Own Bookings    │    ✅    │    ✅    │
│ Create Booking       │    ✅    │    ✅    │
│ Edit Own Booking     │    ✅    │    ✅    │
│ Edit Any Booking     │    ✅    │    ❌    │
│ Delete Own Booking   │    ✅    │    ✅    │
│ Delete Any Booking   │    ✅    │    ❌    │
├──────────────────────┼──────────┼──────────┤
│ View Logs            │    ✅    │    ❌    │
└──────────────────────┴──────────┴──────────┘
```

---

## 📈 STATE DIAGRAM - Booking Status

```
                   ┌─────────────┐
                   │   Create    │
                   │   Booking   │
                   └──────┬──────┘
                          │
                          ▼
                   ┌─────────────┐
              ┌───→│   ACTIVE    │───┐
              │    └─────────────┘   │
              │                      │
    Cancel    │                      │ Complete
              │                      │
              │                      ▼
         ┌────┴────────┐      ┌──────────────┐
         │  CANCELLED  │      │  COMPLETED   │
         └─────────────┘      └──────────────┘
              │                      │
              │                      │
              │    Room becomes      │
              └────► available ◄─────┘
```

---

## 🧪 TEST COVERAGE MAP

```
test_oop.py
  │
  ├─ Test 1: Abstract Class ✅
  │    └─ Verify Room cannot be instantiated
  │
  ├─ Test 2: Inheritance ✅
  │    ├─ Create StandardRoom
  │    ├─ Create DeluxeRoom
  │    └─ Create SuiteRoom
  │
  ├─ Test 3: Polymorphism ✅
  │    ├─ calculate_price() for 2 nights
  │    ├─ calculate_price() for 5 nights
  │    └─ Verify different discounts
  │
  ├─ Test 4: Encapsulation ✅
  │    ├─ Test property getters
  │    └─ Test property setters
  │
  ├─ Test 5: Abstraction ✅
  │    ├─ get_room_type()
  │    ├─ calculate_price()
  │    └─ get_amenities()
  │
  ├─ Test 6: Serialization ✅
  │    └─ to_dict() method
  │
  ├─ Test 7: User Class ✅
  │    ├─ is_admin()
  │    └─ check_password()
  │
  └─ Test 8: Booking Class ✅
       └─ Create booking object
```

---

## 🎯 OOP PRINCIPLES IMPLEMENTATION MAP

```
┌─────────────────────────────────────────────────────────┐
│                  ABSTRACTION                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │ Abstract class Room with abstract methods        │  │
│  │ - get_room_type()                                │  │
│  │ - calculate_price()                              │  │
│  │ - get_amenities()                                │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
            │                  │                  │
            ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────┐
│                   INHERITANCE                           │
│  ┌───────────┐   ┌───────────┐   ┌───────────┐        │
│  │ Standard  │   │  Deluxe   │   │   Suite   │        │
│  │   Room    │   │   Room    │   │   Room    │        │
│  └───────────┘   └───────────┘   └───────────┘        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  POLYMORPHISM                           │
│  Same method name, different implementation:            │
│  - StandardRoom.calculate_price() → base * nights       │
│  - DeluxeRoom.calculate_price() → with 10% discount     │
│  - SuiteRoom.calculate_price() → with 15% discount      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  ENCAPSULATION                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │ Private attributes: _room_id, _room_number, etc  │  │
│  │ Public interface: @property decorators           │  │
│  │ Controlled access via getters/setters            │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

**💡 Gunakan diagram-diagram ini untuk memahami dan menjelaskan implementasi OOP!**
